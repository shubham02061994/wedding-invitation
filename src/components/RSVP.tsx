import { FormEvent, useState } from 'react'
import { Check, X } from 'lucide-react'

type Choice = 'yes' | 'no' | null
export function RSVP() { const [choice, setChoice] = useState<Choice>(null); const [sent, setSent] = useState(false); const [name, setName] = useState(''); const [guests, setGuests] = useState('1'); const [message, setMessage] = useState(''); const submit = (e: FormEvent) => { e.preventDefault(); if (!choice || !name.trim()) return; setSent(true) }
return <section id="rsvp" className="section-pad bg-[#6E1F2E] text-[#FFF9EF]"><div className="mx-auto max-w-xl"><div className="text-center"><p className="text-[10px] uppercase tracking-[0.35em] text-[#d9c18e]">Will you join us?</p><h2 className="mt-4 font-display text-6xl">RSVP</h2><p className="mt-4 font-serif text-sm leading-7 text-white/70">Please let us know if we can count on you.</p></div>{sent ? <div className="mt-10 border border-[#B5965A]/60 bg-[#42131E] p-8 text-center"><p className="font-display text-4xl">Thank you, {name}.</p><p className="mt-3 text-sm text-white/70">Your RSVP has been recorded for this invitation.</p></div> : <form onSubmit={submit} className="mt-10 space-y-6"><div className="grid grid-cols-2 gap-3"><button type="button" onClick={() => setChoice('yes')} className={`min-h-14 border px-4 text-xs uppercase tracking-[0.2em] ${choice === 'yes' ? 'border-[#FFF9EF] bg-[#FFF9EF] text-[#6E1F2E]' : 'border-[#B5965A]/60 text-white'}`}><Check className="mx-auto mb-1" size={17}/>Joyfully yes</button><button type="button" onClick={() => setChoice('no')} className={`min-h-14 border px-4 text-xs uppercase tracking-[0.2em] ${choice === 'no' ? 'border-[#FFF9EF] bg-[#FFF9EF] text-[#6E1F2E]' : 'border-[#B5965A]/60 text-white'}`}><X className="mx-auto mb-1" size={17}/>Regretfully no</button></div><label className="block text-xs uppercase tracking-[0.18em]">Your name<input required value={name} onChange={e => setName(e.target.value)} className="mt-2 min-h-12 w-full border border-[#B5965A]/60 bg-transparent px-4 text-base normal-case tracking-normal text-white placeholder:text-white/35" placeholder="Full name" /></label><label className="block text-xs uppercase tracking-[0.18em]">Guests<select value={guests} onChange={e => setGuests(e.target.value)} className="mt-2 min-h-12 w-full border border-[#B5965A]/60 bg-[#42131E] px-4 text-base normal-case tracking-normal text-white"><option>1</option><option>2</option><option>3</option><option>4</option></select></label><label className="block text-xs uppercase tracking-[0.18em]">A note for the couple<textarea value={message} onChange={e => setMessage(e.target.value)} rows={4} className="mt-2 w-full border border-[#B5965A]/60 bg-transparent p-4 text-base normal-case tracking-normal text-white placeholder:text-white/35" placeholder="Leave a little love..." /></label><button disabled={!choice} className="w-full min-h-14 bg-[#FFF9EF] px-6 text-xs font-medium uppercase tracking-[0.25em] text-[#6E1F2E] disabled:cursor-not-allowed disabled:opacity-40">Send RSVP</button><p className="text-center text-[10px] text-white/50">This demo validates the form locally. Connect your preferred RSVP service before launch.</p></form>}</div></section> }
